spring:
  application:
    name: ProductDomainService
  config:
    name: CONFIGSERVER
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
  main:
    allow-bean-definition-overriding: true
server:
  port: 9090
